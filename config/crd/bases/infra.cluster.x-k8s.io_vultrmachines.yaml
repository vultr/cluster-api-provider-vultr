---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: vultrmachines.infra.cluster.x-k8s.io
spec:
  group: infra.cluster.x-k8s.io
  names:
    kind: VultrMachine
    listKind: VultrMachineList
    plural: vultrmachines
    singular: vultrmachine
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VultrMachine is the Schema for the vultrmachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VultrMachineSpec defines the desired state of VultrMachine
            properties:
              osID:
                description: The ID of the operating system to be installed
                type: integer
              planID:
                description: PlanID is the id of Vultr VPS plan (VPSPLANID).
                type: integer
              providerID:
                description: |-
                  Foo is an example field of VultrMachine. Edit vultrmachine_types.go to remove/update
                  ProviderID is the unique identifer as specified by the cloud provider.
                type: string
              scriptID:
                description: ScriptID is the id of Startup Script (SCRIPTID).
                type: integer
              sshKeyName:
                description: SSHKeyName is the name of the ssh key to attach to the
                  instance.
                type: string
            type: object
          status:
            description: VultrMachineStatus defines the observed state of VultrMachine
            properties:
              powerStatus:
                description: PowerStatus represents whether the server is powered
                  on or not.
                type: string
              ready:
                description: Ready represents the infrastructure is ready to be used
                  or not.
                type: boolean
              serverState:
                description: ServerState represents a more detailed server status.
                type: string
              subscriptionStatus:
                description: ServerStatus represents the status of subscription.
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
